# Serde.FS

## Local debug build

* Run `dotnet fsi debug-systemtextjson.fsx` to build, pack, and test the SystemTextJson backend against the SampleApp.
* Run `dotnet fsi debug-systemtextjson.fsx -- -p clean` to delete debug packages, caches, and kill build processes.
